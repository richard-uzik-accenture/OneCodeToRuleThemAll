VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: set to "true" to bypass Supabase auth with a mock session and
# seed tasks, for local browser automation / UI review without a real login.
VITE_DEV_MODE=
